version: '3'
services:
	web:
		image: teste
		container_name: teste
		build:
			context: .
		volumes:
			- /app:./app
		ports:
			- "8080:8080"
		networks:
			- redinha
networks:
	redinha:
		driver: bridge